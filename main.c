/**
 * Ex2
 * Ori Kopel
 * 205533151
 */
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <string.h>

#define MAX_CHARS 20

int main() {
    char task[MAX_CHARS];
    printf("enter task");

    int execl(const char *path, const char *arg, ...);
    scanf("%s", &task);

    return 0;
}